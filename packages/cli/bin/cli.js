#!/usr/bin/env node

(async () => {
    try {
     const [google, apple] = getGoogleWebsite()
    const

    return google.title
    } catch(e) {

    }
})()

(() => {
    return getGoogleWebsite().then((website) => {
        return website.title
    })
})()